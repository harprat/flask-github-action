name: CD (Continuous Delivery)

on:
  push:
    branches: [ "main" ]

jobs:

  build-push:
    name: Build and Push
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      

      - name: Tag repository
        id: tag
        run: echo "::set-output name=date::$(date +'%s')"

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Images
        env:
            TAG: ${{ steps.date.outputs.tag }}
        run: |
            docker build -t $DOCKERHUB_USERNAME/flaskapp:1.0 ./app
            docker build -t $DOCKERHUB_USERNAME/postgres:1.0 ./postgres

      - name: Docker Push
        run:
            docker push $DOCKERHUB_USERNAME/flaskapp:1.0
            docker push $DOCKERHUB_USERNAME/postgres:1.0

          
